import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />); // root에 App.js를 랜더링 하겠다

// 최종적으로 우리가 index.html에 Class=root에다가 보낸다
