function setInterval() {
  const now = new Date();
  console.log(now);
  const result = ("#clock")

  const date = console.log(now.getUTCDate(), "d :");
  const hours = console.log(now.getUTCHours(), "h :");
  const minsutes = console.log(now.getUTCMinutes(), "m :");
  const seconds = console.log(now.getUTCSeconds(), "s :");
  const result = last result(date,hours,minsutes,seconds);

  const time1 = document.querySelector(".clock");
  console.log(clock);
  const time2 = new Date(2023, 0, 5, 11, 50, 20);
  console.log(time);
}
